# Retained Experiment Evidence

Use one directory per approved retained run, for example `t100_5x10_YYYYMMDD`.
Keep `summary.yaml`, aggregate metrics, fold manifest, paired deltas, and the
threshold evidence necessary for review. Do not add input joblib files or
patient-level CSVs unless data governance explicitly approves them for tracking.
