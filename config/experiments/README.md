# Experiments

`config_0_2_14_pca_denoising.yaml` compares the unchanged Aramina 0.2.14
100-bin profile with Smoothed PCA and Sparse PCA reconstructions. The denoiser
is fitted inside each patient-safe training fold. Outputs are research artifacts
and are not eligible for product promotion without a separate model decision.
